/* ============================================================
   OPUS NO. 1: VIENNA — living digital booklet
   Single visual world: obsidian / bronze / gold, controlled
   electric-blue accent for the trance transformation.
   Deliberately single-theme (concert hall at night).
   ============================================================ */

:root{
  --void:        #07080b;
  --surface:     #0e1015;
  --surface-2:   #14161d;
  --surface-3:   #1b1e28;
  --line:        #2a2e3a;
  --line-soft:   rgba(242,237,226,0.08);
  --ivory:       #f2ede2;
  --ivory-dim:   #b8b1a2;
  --muted:       #7d8293;
  --gold:        #c9a24b;
  --gold-bright: #e8cf8a;
  --gold-deep:   #7d5f28;
  --bronze:      #a9793f;
  --blue:        #5ea8e8;
  --blue-bright: #8fc8f7;
  --blue-deep:   #17263a;
  --blue-glow:   rgba(94,168,232,0.30);

  --f-display: 'Playfair Display', Georgia, 'Times New Roman', serif;
  --f-head:    'Cinzel', Georgia, serif;
  --f-body:    'Cormorant Garamond', Georgia, serif;
  --f-mono:    'Space Grotesk', 'Segoe UI', system-ui, sans-serif;

  --maxw: 1180px;
  --pad-x: clamp(1.25rem, 4vw, 3.5rem);
  --pad-y: clamp(5rem, 11vw, 9rem);

  color-scheme: dark;
}

*,*::before,*::after{ box-sizing:border-box; }
html{ scroll-behavior:smooth; }
@media (prefers-reduced-motion: reduce){
  html{ scroll-behavior:auto; }
  *{ animation-duration:0.001ms !important; animation-iteration-count:1 !important; transition-duration:0.001ms !important; scroll-behavior:auto !important; }
}

body{
  margin:0;
  background:var(--void);
  color:var(--ivory-dim);
  font-family:var(--f-body);
  font-size:1.15rem;
  line-height:1.65;
  -webkit-font-smoothing:antialiased;
}

img,svg{ max-width:100%; display:block; }
a{ color:inherit; }
::selection{ background:var(--gold-deep); color:var(--ivory); }

.wrap{ max-width:var(--maxw); margin:0 auto; padding-left:var(--pad-x); padding-right:var(--pad-x); }
.section{ position:relative; padding-top:var(--pad-y); padding-bottom:var(--pad-y); }
.section-inner{ max-width:var(--maxw); margin:0 auto; padding-left:var(--pad-x); padding-right:var(--pad-x); }

h1,h2,h3,h4{ margin:0; font-weight:600; color:var(--ivory); text-wrap:balance; }

.eyebrow{
  font-family:var(--f-mono); font-size:0.72rem; font-weight:600;
  letter-spacing:0.32em; text-transform:uppercase; color:var(--blue);
  display:inline-flex; align-items:center; gap:0.6em;
}
.eyebrow::before{ content:""; width:1.6em; height:1px; background:var(--blue); opacity:0.7; }
.eyebrow.gold{ color:var(--gold); }
.eyebrow.gold::before{ background:var(--gold); }

.title-xl{
  font-family:var(--f-display); font-weight:700; font-style:italic;
  font-size:clamp(2.4rem, 6vw, 4.6rem); line-height:1.04; color:var(--ivory);
  margin-top:0.5rem;
}
.title-lg{
  font-family:var(--f-head); font-weight:600; letter-spacing:0.02em;
  font-size:clamp(1.7rem, 3.4vw, 2.5rem); color:var(--gold-bright);
}
p{ margin:0 0 1rem; max-width:62ch; }
.lede{ font-size:clamp(1.2rem,1.6vw,1.45rem); color:var(--ivory); font-style:italic; max-width:56ch; }

.gold-rule{ width:64px; height:1.5px; background:linear-gradient(90deg,var(--gold),transparent); margin:1.5rem 0; }
.gold-rule.center{ margin-left:auto; margin-right:auto; }

.btn{
  font-family:var(--f-mono); font-size:0.78rem; font-weight:600; letter-spacing:0.14em; text-transform:uppercase;
  color:var(--void); background:linear-gradient(180deg,var(--gold-bright),var(--gold));
  border:none; padding:0.9em 1.6em; border-radius:2px; cursor:pointer;
  text-decoration:none; display:inline-flex; align-items:center; gap:0.6em;
  transition:transform 0.25s ease, box-shadow 0.25s ease;
}
.btn:hover{ transform:translateY(-1px); box-shadow:0 8px 24px -8px var(--blue-glow); }
.btn:focus-visible, a:focus-visible, button:focus-visible{ outline:2px solid var(--blue-bright); outline-offset:3px; }
.btn.ghost{
  background:transparent; color:var(--ivory); border:1px solid var(--line-soft);
}
.btn.ghost:hover{ border-color:var(--blue); color:var(--blue-bright); }

/* ---------- nav ---------- */
.nav{
  position:fixed; top:0; left:0; right:0; z-index:100;
  display:flex; align-items:center; justify-content:space-between;
  padding:1.1rem var(--pad-x);
  background:linear-gradient(180deg, rgba(7,8,11,0.92), rgba(7,8,11,0));
  backdrop-filter:blur(6px);
  opacity:0; transform:translateY(-8px); pointer-events:none;
  transition:opacity 0.4s ease, transform 0.4s ease, background 0.3s ease;
}
.nav.visible{ opacity:1; transform:translateY(0); pointer-events:auto; }
.nav.solid{ background:rgba(9,10,14,0.86); border-bottom:1px solid var(--line-soft); }
.nav-mark{ font-family:var(--f-head); font-size:0.82rem; letter-spacing:0.18em; color:var(--ivory); text-decoration:none; white-space:nowrap; }
.nav-mark b{ color:var(--gold-bright); font-weight:600; }
.nav-links{ display:flex; gap:1.6rem; list-style:none; margin:0; padding:0; }
.nav-links a{
  font-family:var(--f-mono); font-size:0.68rem; letter-spacing:0.14em; text-transform:uppercase;
  color:var(--muted); text-decoration:none; padding:0.3em 0; border-bottom:1px solid transparent;
  transition:color 0.2s ease, border-color 0.2s ease;
}
.nav-links a:hover, .nav-links a.active{ color:var(--blue-bright); border-color:var(--blue); }
.nav-toggle{ display:none; }
@media (max-width: 860px){
  .nav-links{ display:none; }
}

/* ---------- hero ---------- */
.hero{
  position:relative; min-height:100svh; display:flex; flex-direction:column;
  justify-content:space-between; align-items:center; text-align:center;
  padding:clamp(5rem,10vw,7rem) var(--pad-x) 3rem;
  overflow:hidden;
  background:
    radial-gradient(1100px 620px at 50% -8%, rgba(26,20,32,0.7) 0%, transparent 55%),
    radial-gradient(900px 700px at 82% 105%, rgba(23,38,58,0.5) 0%, transparent 60%),
    linear-gradient(180deg, rgba(10,11,15,0.45) 0%, rgba(8,9,12,0.7) 55%, rgba(7,8,11,0.94) 100%),
    url('/releases/opus-no-1-vienna-cover.png');
  background-size: auto, auto, auto, cover;
  background-position: center, center, center, center;
  background-repeat: no-repeat;
}
.hero-arches{ position:absolute; inset:0; z-index:0; opacity:0.55; }
.hero-beam{
  position:absolute; left:50%; top:0; width:340px; height:100%;
  transform:translateX(-50%);
  background:linear-gradient(180deg, rgba(232,207,138,0.16), rgba(94,168,232,0.10) 55%, transparent 85%);
  filter:blur(6px); pointer-events:none;
}
.hero-content{ position:relative; z-index:2; }
.hero .eyebrow{ justify-content:center; }
.hero .kicker{
  font-family:var(--f-mono); font-size:0.8rem; letter-spacing:0.5em; text-transform:uppercase;
  color:var(--blue-bright); margin-bottom:1.4rem;
}
.hero-title{ font-family:var(--f-display); color:var(--ivory); line-height:0.98; }
.hero-title .op{
  display:block; font-style:normal; font-weight:600; letter-spacing:0.06em;
  font-size:clamp(1.1rem,2.6vw,1.6rem); color:var(--gold-bright); margin-bottom:0.6rem;
}
.hero-title .main{
  display:block; font-style:italic; font-weight:900;
  font-size:clamp(3.4rem,11vw,8rem);
}
.hero-sub{
  font-family:var(--f-head); font-size:clamp(0.95rem,1.6vw,1.25rem); letter-spacing:0.08em;
  color:var(--gold-bright); margin-top:1.6rem;
}
.hero-movements{
  margin-top:2.4rem; display:flex; flex-wrap:wrap; justify-content:center; gap:0.5rem 1.6rem;
  font-family:var(--f-mono); font-size:0.72rem; letter-spacing:0.14em; text-transform:uppercase; color:var(--muted);
}
.hero-cta{ margin-top:2.6rem; display:flex; gap:1rem; flex-wrap:wrap; justify-content:center; position:relative; z-index:2; }
.hero-foot{ position:relative; z-index:2; display:flex; flex-direction:column; align-items:center; gap:1.2rem; }
.hero-artist{ font-family:var(--f-head); font-weight:600; letter-spacing:0.2em; font-size:0.95rem; color:var(--ivory); }
.hero-place{ font-family:var(--f-mono); font-size:0.7rem; letter-spacing:0.3em; color:var(--muted); margin-top:0.3rem; }
.scroll-cue{
  width:1px; height:44px; background:linear-gradient(180deg, var(--gold), transparent);
  position:relative; opacity:0.7;
  animation: cue 2.4s ease-in-out infinite;
}
@keyframes cue{ 0%,100%{ opacity:0.25; } 50%{ opacity:0.9; } }

/* reveal-on-scroll */
.reveal{ opacity:0; transform:translateY(28px); transition:opacity 0.9s cubic-bezier(.2,.7,.2,1), transform 0.9s cubic-bezier(.2,.7,.2,1); }
.reveal.in{ opacity:1; transform:none; }
.reveal-stagger > *{ opacity:0; transform:translateY(20px); transition:opacity 0.7s ease, transform 0.7s ease; }
.reveal-stagger.in > *{ opacity:1; transform:none; }
.reveal-stagger.in > *:nth-child(1){ transition-delay:0.02s; }
.reveal-stagger.in > *:nth-child(2){ transition-delay:0.10s; }
.reveal-stagger.in > *:nth-child(3){ transition-delay:0.18s; }
.reveal-stagger.in > *:nth-child(4){ transition-delay:0.26s; }
.reveal-stagger.in > *:nth-child(5){ transition-delay:0.34s; }
.reveal-stagger.in > *:nth-child(6){ transition-delay:0.42s; }

/* divider motif — architectural arch line */
.arch-divider{ display:flex; justify-content:center; padding:0 0 var(--pad-y); margin-top:calc(-1 * var(--pad-y) + 2rem); }
.arch-divider svg{ width:min(560px, 80%); height:auto; opacity:0.55; }

footer.site-footer{
  border-top:1px solid var(--line-soft); padding:3rem var(--pad-x) 6rem; text-align:center;
  font-family:var(--f-mono); font-size:0.72rem; letter-spacing:0.08em; color:var(--muted);
}

/* ---------- Four Movements ---------- */
#movements{ background:var(--surface); border-top:1px solid var(--line-soft); }
.movement-grid{
  display:grid; grid-template-columns:repeat(4,1fr); gap:1px; margin-top:3rem;
  background:var(--line-soft); border:1px solid var(--line-soft);
}
.movement-cell{
  background:var(--surface); padding:2.4rem 1.6rem; text-align:center;
  display:flex; flex-direction:column; align-items:center; gap:0.8rem;
  transition:background 0.3s ease;
}
.movement-cell:hover{ background:var(--surface-2); }
.movement-cell .mnum{ font-family:var(--f-display); font-style:italic; font-weight:700; font-size:2.4rem; color:var(--gold-deep); transition:color 0.3s ease; }
.movement-cell:hover .mnum{ color:var(--gold-bright); }
.movement-cell .mtitle{ font-family:var(--f-head); font-size:1rem; letter-spacing:0.03em; color:var(--ivory); }
.movement-cell .msub{ font-family:var(--f-mono); font-size:0.68rem; letter-spacing:0.16em; text-transform:uppercase; color:var(--blue); }
.movement-cell .micon{ width:34px; height:34px; opacity:0.85; margin-bottom:0.2rem; }
@media (max-width:760px){ .movement-grid{ grid-template-columns:1fr 1fr; } }

.journey-strip{
  display:flex; align-items:center; justify-content:center; gap:0.8rem; margin-top:3rem; flex-wrap:wrap;
  font-family:var(--f-mono); font-size:0.7rem; letter-spacing:0.12em; text-transform:uppercase; color:var(--muted);
}
.journey-strip .node{ display:flex; flex-direction:column; align-items:center; gap:0.5rem; }
.journey-strip .dot{ width:10px; height:10px; border-radius:50%; background:var(--gold); box-shadow:0 0 12px 2px var(--blue-glow); }
.journey-strip .arrow{ color:var(--line); font-size:1rem; }

/* ---------- The Theme ---------- */
#theme{ background:var(--void); }
.theme-panel{
  margin-top:2.5rem; background:var(--surface-2); border:1px solid var(--line-soft); border-radius:3px;
  padding:clamp(1.2rem,3vw,2.6rem); overflow-x:auto;
}
.theme-caption{ display:flex; flex-wrap:wrap; gap:2rem; margin-top:2rem; }
.theme-caption .col{ flex:1; min-width:220px; }
.theme-caption h4{ font-family:var(--f-mono); font-size:0.7rem; letter-spacing:0.16em; text-transform:uppercase; color:var(--blue); margin-bottom:0.6rem; }
.theme-caption p{ font-size:1.02rem; }

/* ---------- From Orchestra to Trance ---------- */
#orchestra-trance{ background:var(--surface); border-top:1px solid var(--line-soft); }
.ot-frame{ display:grid; grid-template-columns:1fr 28px; gap:1.5rem; margin-top:3rem; }
.ot-rows{ display:flex; flex-direction:column; }
.ot-row{
  display:grid; grid-template-columns:34px 168px 1fr 150px; gap:1.4rem; align-items:center;
  padding:1.15rem 0; border-bottom:1px solid var(--line-soft);
}
.ot-row:last-child{ border-bottom:none; }
.ot-icon{ width:26px; height:26px; opacity:0.9; }
.ot-name{ font-family:var(--f-head); font-size:0.86rem; letter-spacing:0.04em; color:var(--ivory); }
.ot-desc{ font-size:0.98rem; margin:0; max-width:46ch; color:var(--ivory-dim); }
.ot-wave{ width:100%; height:28px; }
.ot-row.is-electronic .ot-name{ color:var(--blue-bright); }

.ot-axis{
  display:flex; flex-direction:column; align-items:center; justify-content:space-between;
  position:relative; padding:0.4rem 0;
}
.ot-axis::before{
  content:""; position:absolute; top:0; bottom:0; left:50%; width:1px; transform:translateX(-50%);
  background:linear-gradient(180deg, var(--gold), var(--blue));
}
.ot-axis span{
  writing-mode:vertical-rl; font-family:var(--f-mono); font-size:0.62rem; letter-spacing:0.2em;
  text-transform:uppercase; background:var(--surface); padding:0.4rem 0; color:var(--muted); position:relative; z-index:1;
}
.ot-tagline{
  text-align:center; margin-top:2.6rem; font-family:var(--f-mono); font-size:0.72rem; letter-spacing:0.2em;
  text-transform:uppercase; color:var(--gold-bright);
}
@media (max-width:760px){
  .ot-row{ grid-template-columns:28px 1fr; grid-template-areas:"icon name" "desc desc" "wave wave"; row-gap:0.5rem; }
  .ot-icon{ grid-area:icon; } .ot-name{ grid-area:name; } .ot-desc{ grid-area:desc; max-width:none; } .ot-wave{ grid-area:wave; }
}

/* ---------- Virtual Orchestra — Stage Layout ---------- */
#stage{ background:var(--void); }
.stage-panel{
  margin-top:2.5rem; background:var(--surface-2); border:1px solid var(--line-soft); border-radius:3px;
  padding:clamp(1rem,3vw,2.2rem); overflow-x:auto;
}
.stage-panel svg{ width:min(640px,100%); margin:0 auto; }
.stage-legend{ display:flex; flex-wrap:wrap; gap:0.5rem 1.6rem; margin-top:1.6rem; justify-content:center; }
.stage-legend .item{ display:flex; align-items:center; gap:0.5rem; font-family:var(--f-mono); font-size:0.7rem; letter-spacing:0.08em; text-transform:uppercase; color:var(--ivory-dim); }
.stage-legend .sw{ width:11px; height:11px; border-radius:50%; }
.stage-note{
  max-width:70ch; margin:2.4rem auto 0; text-align:center; font-style:italic; color:var(--ivory);
}
.about-grid{ display:grid; grid-template-columns:1.1fr 0.9fr; gap:3rem; margin-top:4rem; align-items:start; }
.about-grid .credits-mini{
  background:var(--surface-2); border:1px solid var(--line-soft); border-radius:3px; padding:1.8rem 2rem;
}
.about-grid .credits-mini h4{ font-family:var(--f-mono); font-size:0.68rem; letter-spacing:0.16em; text-transform:uppercase; color:var(--blue); margin-bottom:0.9rem; }
.about-grid .credits-mini dl{ margin:0; }
.about-grid .credits-mini dt{ font-family:var(--f-mono); font-size:0.66rem; letter-spacing:0.08em; text-transform:uppercase; color:var(--muted); margin-top:0.9rem; }
.about-grid .credits-mini dd{ margin:0.15rem 0 0; color:var(--ivory); font-size:1rem; }
@media (max-width:820px){ .about-grid{ grid-template-columns:1fr; } }

/* ---------- Artistic Note ---------- */
#artistic-note{ background:var(--surface); border-top:1px solid var(--line-soft); border-bottom:1px solid var(--line-soft); }
.note-layout{ display:grid; grid-template-columns:0.9fr 1.1fr; gap:4rem; align-items:center; }
.note-tenets{ display:flex; flex-direction:column; gap:0.6rem; margin-top:2rem; }
.note-tenets span{ font-family:var(--f-head); font-size:1.05rem; letter-spacing:0.03em; color:var(--gold-bright); }
@media (max-width:820px){ .note-layout{ grid-template-columns:1fr; gap:2rem; } }

/* ---------- Movement deep-dives ---------- */
.movement-deep{ position:relative; overflow:hidden; border-top:1px solid var(--line-soft); }
.movement-deep .section-inner{ display:grid; grid-template-columns:220px 1fr; gap:3rem; align-items:start; }
.movement-deep .mtag{ position:sticky; top:8rem; }
.movement-deep .mtag .mnum{ font-family:var(--f-display); font-style:italic; font-weight:700; font-size:4.5rem; line-height:1; }
.movement-deep .mtag .mroman{ font-family:var(--f-mono); font-size:0.7rem; letter-spacing:0.2em; text-transform:uppercase; margin-top:0.6rem; }
.movement-deep .mbody .title-lg{ font-size:clamp(1.9rem,4vw,2.8rem); }
.movement-deep .msubtitle{ font-family:var(--f-mono); font-size:0.72rem; letter-spacing:0.24em; text-transform:uppercase; margin:0.6rem 0 1.6rem; }
.movement-deep .mtext{ font-size:1.2rem; max-width:60ch; }
.movement-deep .mglow{ position:absolute; inset:0; pointer-events:none; z-index:0; }
.movement-deep .section-inner{ position:relative; z-index:1; }
@media (max-width:700px){ .movement-deep .section-inner{ grid-template-columns:1fr; gap:1.2rem; } .movement-deep .mtag{ position:static; display:flex; align-items:baseline; gap:1rem; } }

#m1{ background:linear-gradient(180deg, var(--void) 0%, #0b0c10 100%); }
#m1 .mnum, #m1 .mroman{ color:var(--gold-bright); }
#m2{ background:linear-gradient(135deg, #0c0d11 0%, #0e131c 60%, #101a26 100%); }
#m2 .mnum{ color:var(--gold-bright); } #m2 .mroman{ color:var(--blue); }
#m3{ background:linear-gradient(135deg, #0d1420 0%, #122036 55%, #16283f 100%); }
#m3 .mnum, #m3 .mroman{ color:var(--blue-bright); }
#m4{ background:linear-gradient(180deg, #0e131a 0%, var(--void) 100%); }
#m4 .mnum, #m4 .mroman{ color:var(--gold-bright); }
.mglow{ filter:blur(60px); }
#m1 .mglow{ background:radial-gradient(420px 320px at 14% 40%, rgba(201,162,75,0.14), transparent 70%); }
#m2 .mglow{ background:radial-gradient(460px 340px at 14% 45%, rgba(94,168,232,0.16), transparent 70%), radial-gradient(300px 260px at 80% 30%, rgba(201,162,75,0.08), transparent 70%); }
#m3 .mglow{ background:radial-gradient(560px 420px at 20% 50%, rgba(94,168,232,0.26), transparent 70%), radial-gradient(380px 300px at 85% 20%, rgba(201,162,75,0.10), transparent 70%); }
#m4 .mglow{ background:radial-gradient(420px 320px at 14% 40%, rgba(201,162,75,0.14), transparent 70%); }

/* ---------- Credits & Tracklist ---------- */
#credits{ background:var(--surface); border-top:1px solid var(--line-soft); }
.credits-layout{ display:grid; grid-template-columns:1.15fr 0.85fr; gap:4rem; margin-top:3rem; align-items:start; }
.tracklist{ border-top:1px solid var(--line-soft); }
.track-row{
  display:grid; grid-template-columns:2.4rem 1fr auto; gap:1rem; align-items:baseline;
  padding:1.1rem 0; border-bottom:1px solid var(--line-soft);
}
.track-row .tnum{ font-family:var(--f-head); color:var(--gold); font-size:0.95rem; }
.track-row .tname{ font-family:var(--f-body); font-size:1.15rem; color:var(--ivory); }
.track-row .tname small{ display:block; font-family:var(--f-mono); font-size:0.66rem; letter-spacing:0.1em; text-transform:uppercase; color:var(--blue); margin-top:0.25rem; }
.track-row .tdur{ font-family:var(--f-mono); font-size:0.85rem; color:var(--muted); font-variant-numeric:tabular-nums; }
.credits-list dt{ font-family:var(--f-mono); font-size:0.66rem; letter-spacing:0.1em; text-transform:uppercase; color:var(--muted); margin-top:1.1rem; }
.credits-list dt:first-child{ margin-top:0; }
.credits-list dd{ margin:0.2rem 0 0; color:var(--ivory); font-size:1.05rem; }
.rights-note{ margin-top:2rem; font-family:var(--f-mono); font-size:0.72rem; letter-spacing:0.04em; color:var(--muted); }
@media (max-width:820px){ .credits-layout{ grid-template-columns:1fr; gap:2.4rem; } }

/* ---------- Listen / Player ---------- */
#listen{ background:var(--void); border-top:1px solid var(--line-soft); }
.player{
  margin-top:3rem; background:linear-gradient(160deg, var(--surface-2), var(--surface-3));
  border:1px solid var(--line-soft); border-radius:4px; padding:clamp(1.4rem,3vw,2.4rem);
}
.player-transport{ display:flex; align-items:center; gap:1.2rem; }
.play-btn{
  flex:0 0 auto; width:56px; height:56px; border-radius:50%; border:1px solid var(--gold-deep);
  background:radial-gradient(circle at 35% 30%, var(--gold-bright), var(--gold) 60%, var(--gold-deep));
  color:var(--void); display:flex; align-items:center; justify-content:center; cursor:pointer;
  font-size:1.1rem; transition:box-shadow 0.25s ease, transform 0.2s ease;
}
.play-btn:hover{ box-shadow:0 0 0 6px rgba(201,162,75,0.14); transform:scale(1.03); }
.player-meta{ flex:1; min-width:0; }
.player-meta .ptitle{ font-family:var(--f-head); font-size:1rem; color:var(--ivory); white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
.player-meta .psub{ font-family:var(--f-mono); font-size:0.68rem; letter-spacing:0.1em; text-transform:uppercase; color:var(--blue); margin-top:0.2rem; }
.player-time{ font-family:var(--f-mono); font-size:0.75rem; color:var(--muted); font-variant-numeric:tabular-nums; white-space:nowrap; }
.seek{
  -webkit-appearance:none; appearance:none; width:100%; height:3px; margin-top:1.3rem;
  background:linear-gradient(90deg, var(--gold) 0%, var(--gold) var(--prog,0%), var(--line) var(--prog,0%), var(--line) 100%);
  border-radius:2px; cursor:pointer;
}
.seek::-webkit-slider-thumb{ -webkit-appearance:none; width:12px; height:12px; border-radius:50%; background:var(--gold-bright); border:1px solid var(--void); margin-top:-4.5px; }
.seek::-moz-range-thumb{ width:12px; height:12px; border-radius:50%; background:var(--gold-bright); border:1px solid var(--void); }
.seek::-moz-range-track{ height:3px; background:var(--line); border-radius:2px; }
.player-status{ margin-top:0.9rem; font-family:var(--f-mono); font-size:0.68rem; letter-spacing:0.06em; color:var(--muted); min-height:1.2em; }
.player-tracks{ margin-top:1.8rem; border-top:1px solid var(--line-soft); }
.player-track{
  display:grid; grid-template-columns:2rem 1fr auto; gap:1rem; align-items:center;
  padding:0.9rem 0.4rem; border-bottom:1px solid var(--line-soft); cursor:pointer; border-radius:2px;
  transition:background 0.2s ease;
}
.player-track:hover{ background:rgba(242,237,226,0.03); }
.player-track.active{ background:rgba(94,168,232,0.06); }
.player-track.active .pt-num{ color:var(--blue-bright); }
.player-track .pt-num{ font-family:var(--f-head); color:var(--gold); font-size:0.9rem; }
.player-track .pt-name{ font-family:var(--f-body); font-size:1.05rem; color:var(--ivory); }
.player-track .pt-dur{ font-family:var(--f-mono); font-size:0.78rem; color:var(--muted); font-variant-numeric:tabular-nums; }

.platform-links{ display:flex; flex-wrap:wrap; gap:0.9rem; margin-top:2.6rem; }
.platform-link{
  display:flex; align-items:center; gap:0.6rem; padding:0.7em 1.1em; border:1px solid var(--line-soft); border-radius:2px;
  font-family:var(--f-mono); font-size:0.74rem; letter-spacing:0.08em; text-transform:uppercase; color:var(--ivory-dim);
  text-decoration:none; transition:border-color 0.2s ease, color 0.2s ease;
  background:transparent; cursor:pointer;
}
.platform-link svg{ width:18px; height:18px; }
.platform-link:hover{ border-color:var(--blue); color:var(--blue-bright); }
.platform-note{ margin-top:1rem; font-family:var(--f-mono); font-size:0.68rem; color:var(--muted); }
